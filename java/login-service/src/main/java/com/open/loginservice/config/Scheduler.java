package com.open.loginservice.config;

import org.springframework.stereotype.Component;

@Component
public class Scheduler {
	
	//every 1 minute
//	@Scheduled(cron = "0 0/1 * * * ?")
//	@Scheduled(cron = "0 0 1 * * ?")
	public void scheduler() {
//		examService.checkExamCompletionStatus();
	}
}
